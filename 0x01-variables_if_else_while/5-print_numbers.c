#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int digit;

digit = 0;
for (digit = 0; digit < 10; digit++)
{
  printf("%i", digit); }
printf("\n");
return (0);
}
