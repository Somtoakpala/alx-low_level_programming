#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alpha;

alpha = '0';
for (alpha = '0'; alpha < '10'; alpha++)
{
putchar(alpha);
} }
putchar('\n');
return (0);
}
